{
  "name": "tests",
  "$schema": "../../node_modules/nx/schemas/project-schema.json",
  "sourceRoot": "packages/tests/src",
  "projectType": "library",
  "tags": [
    "scope:tests"
  ],
  "targets": {
    "test": {
      "executor": "@nx/jest:jest",
      "outputs": [
        "{workspaceRoot}/coverage/{projectRoot}"
      ],
      "options": {
        "jestConfig": "packages/tests/jest.config.ts"
      }
    },
    "test:integration": {
      "executor": "@nx/jest:jest",
      "outputs": [
        "{workspaceRoot}/coverage/{projectRoot}-integration"
      ],
      "options": {
        "jestConfig": "packages/tests/jest.integration.config.ts"
      }
    }
  }
}
